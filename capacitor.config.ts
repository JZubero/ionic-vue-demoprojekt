import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'my-vue-ionic-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
